{
  "name": "gravity-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Professional foundation for the Gravity AI Windows assistant.",
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "apps/desktop",
    "packages/shared"
  ],
  "scripts": {
    "build": "npm run build --workspace @gravity-ai/shared && npm run build --workspace @gravity-ai/desktop",
    "dev": "npm run dev --workspace @gravity-ai/desktop",
    "test": "npm run test:run --workspace @gravity-ai/desktop",
    "typecheck": "npm run typecheck --workspace @gravity-ai/shared && npm run typecheck --workspace @gravity-ai/desktop",
    "backend:test": "python scripts/run_backend_tests.py",
    "backend:dev": "python scripts/run_backend_api.py",
    "tauri:dev": "npm run tauri:dev --workspace @gravity-ai/desktop"
  }
}

