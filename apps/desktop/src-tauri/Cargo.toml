[package]
name = "gravity-ai"
version = "0.1.0"
description = "Gravity AI desktop shell"
authors = ["Gravity AI"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"

[features]
custom-protocol = ["tauri/custom-protocol"]
